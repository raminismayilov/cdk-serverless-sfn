version: 0.2

phases:
  install:
    commands:
      - npm install -g npm
      - npm install -g aws-cdk
      - npm install
  build:
    commands:
      - npm install
      - npm run test:infra
      - npm run cdk:synth

artifacts:
  base-directory: infra/cdk.out
  files:
    - '**/*'